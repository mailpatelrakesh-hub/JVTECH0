rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global safety catch-all
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper to validate IDs to prevent resource poisoning
    function isValidId(id) { 
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$'); 
    }

    // Candidates collection rules
    match /candidates/{candidateId} {
      allow get: if isValidId(candidateId);
      allow list: if true;
      allow write: if isValidId(candidateId);
    }

    // Settings collection rules
    match /settings/{settingsId} {
      allow get: if isValidId(settingsId);
      allow list: if true;
      allow write: if isValidId(settingsId);
    }

    // Users collection rules
    match /users/{userId} {
      allow get: if isValidId(userId);
      allow list: if true;
      allow write: if isValidId(userId);
    }

    // Interviews collection rules
    match /interviews/{interviewId} {
      allow get: if isValidId(interviewId);
      allow list: if true;
      allow write: if isValidId(interviewId);
    }
  }
}
